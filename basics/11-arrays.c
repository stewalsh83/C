#include <stdio.h>
#include <stdlib.h>

int main()
{

    int luckyNumbers[10]; // num of elements 10
    luckyNumbers[1] = 80; // assign value to each index
    luckyNumbers[0] = 90;
    printf("%d", luckyNumbers[0]);

    return 0;
}

// int luckyNumbers[] = {2, 4, 6, 8, 10, 12};

// modifiy selected index
// luckyNumbers[1] = 200;
// printf("%d\n", luckyNumbers[1]);

// print index of arrary
// printf("%d", luckyNumbers[2]);

// char phrase[] = "Hello World";
// char phrase[20] = "Array"; // num of elements 20